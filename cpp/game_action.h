/* An action made by a player in a game of Adrestia. One is needed from all
 * players before we can compute the next GameState. */

#pragma once

#include <vector>

typedef std::vector<std::string> GameAction;
